[pytest]
testpaths = tests
asyncio_mode = auto
# One event loop for the whole session, so the session-scoped database
# engine and the tests that use it always run on the same loop.
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
